name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Cache files
        uses: actions/cache@v2
        with:
            path: |
                ~/.sonar
            key: ${{ runner.os }}-sonar
            restore-keys: ${{ runner.os }}-sonar
      - name: Run Codescan On Push
        if: github.event_name == 'push'
        uses: Aradhana-Singh/temp-action@main
        with:
          codeScanUrl: https://preview.codescan.io/
          generateSarifFile: false
          organization: preview-aradhana-org
          projectKey: small-sample1      
          login: e4837ba9d3471232ca7d7b8dc8b7c31400e37da9
      - name: Run Codescan On PR
        if: github.event_name == 'pull_request'
        uses: Aradhana-Singh/temp-action@main
        with:
          codeScanUrl: https://preview.codescan.io/
          generateSarifFile: false
          organization: preview-aradhana-org
          projectKey: small-sample1
          login: e4837ba9d3471232ca7d7b8dc8b7c31400e37da9
          args: |
            sonar.branch.name=${{github.head_ref}}
            sonar.branch.target=${{github.base_ref}}
            sonar.branch.type=SHORT
      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v1
        with:
          sarif_file: codescan.sarif
